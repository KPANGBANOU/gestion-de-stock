

import 'package:flutter/material.dart';

class ContactsEntreprise extends StatelessWidget {
  const ContactsEntreprise({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
